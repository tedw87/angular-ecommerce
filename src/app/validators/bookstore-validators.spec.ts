import { BookstoreValidators } from './bookstore-validators';

describe('BookstoreValidators', () => {
  it('should create an instance', () => {
    expect(new BookstoreValidators()).toBeTruthy();
  });
});
